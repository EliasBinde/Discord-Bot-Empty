module.exports = {
    name: 'template',
    description: 'Describe the command',
    execute: async (msg, args) => {

    
    
        //array with required args
        let required_args = ['argument', 'argument']





        
        run = async (msg, args) => {

            //
            msg.channel.send("This should be run ig not testetd tho")
        }
    },
}